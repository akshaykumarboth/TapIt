<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.akshayboth.tapit">
    
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EnterPayActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".PayAmountActivity" />
        <activity android:name=".SummaryPayActivity" />
        <activity android:name=".ScanActivity" />
        <activity android:name=".DashboardActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".budget.BudgetActivity" />
        <activity android:name=".ScheduleActivity" />
        <activity android:name=".transaction.TransactionsActivity" />
        <activity android:name=".BankAccountsActivity"></activity>




        <meta-data android:name="com.google.android.gms.vision.DEPENDENCIES" android:value="barcode"/>
    </application>

</manifest>